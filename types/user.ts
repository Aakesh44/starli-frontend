export interface IUser {
    email: string;
    name: string;
    id: string;
    // add other user properties as needed
}

export interface User {
    id: string;
    email: string;
    name: string;
    picture: string;
}